# Security
[School Project] Java folder monitoring tool

Encrypts every file placed into it with AES-128 or 256 encryption method, using salt and random bytes from /dev/random to generate a password.

